# 炁（Qi）— 修仙挂机小游戏 | Cursor AI 开发规范

## 项目概况
- **类型**：修仙挂机小游戏
- **引擎**：Cocos Creator 3.8.6+
- **平台**：抖音小游戏（720×1280 竖屏）
- **语言**：TypeScript
- **核心循环**：挂机修炼 → 累积修为 → 突破境界 → 解锁功能 → 变强 → 更快修炼

## 目录规范
```
项目根目录/
├── .cursorrules              # 本文件
├── TODOLIST.md               # 开发进度追踪（必须存在）
├── docs/
│   └── design/
│       ├── GDD.md            # 游戏设计文档
│       ├── levels/           # 关卡设计
│       │   └── dungeons.md   # 秘境关卡详细设计
│       ├── balance/          # 数值设计
│       │   └── numbers.md    # 数值表与计算公式
│       └── references/       # AI生成的参考图
│           ├── ui-*.png      # UI设计参考图
│           └── art-*.png     # 美术风格参考图
├── assets/
│   └── scripts/              # 游戏脚本（已有结构，不改动）
└── settings/                 # Cocos项目设置
```

## 开发流程（严格遵守）

### Step 1: 读取进度
每次对话开始，先读取 `TODOLIST.md`，确认当前进度和下一步任务。

### Step 2: 设计先行
- 如果 `docs/design/GDD.md` 不存在，先根据项目信息生成
- 如果任务涉及新系统/面板，先在 `docs/design/` 下补充设计文档
- 数值相关改动，先更新 `docs/design/balance/numbers.md`

### Step 3: UI设计流程（MCP自动化）
1. 使用 AI生图MCP 生成 UI参考图，保存到 `docs/design/references/ui-*.png`
2. AI 分析参考图，理解布局和视觉层次
3. 通过 **Cocos MCP** 在编辑器中创建场景节点树、设置属性、布局
4. 代码逻辑与编辑器节点绑定

### Step 4: 编码规范
- TypeScript strict mode
- 继承 `Component`，使用 `@ccclass` 装饰器
- 模块间通信统一走 `EventBus`（`GameEvent` 枚举）
- 数据模型使用 `PlayerData`，存档走 `SaveManager`
- 新增事件必须追加到 `GameEvent` 枚举
- 大数字显示用 `NumberUtils.formatBigNumber()`
- 禁止 `any` 类型，禁止 `console.log`（用 `console.warn`/`console.error`）

### Step 5: 验证
- Cocos MCP 日志同步报错信息
- 如有 Playwright MCP，截图对比 `docs/design/references/` 中的参考图
- 验证通过后更新 TODOLIST.md

### Step 6: 更新进度
- 每完成一个任务项，立即在 `TODOLIST.md` 中标记 ✅
- 新发现的任务追加到对应 Phase 下
- 如果 README.md 中的待办清单也需要同步更新

## TODOLIST 管理规则
- 项目首次对话时，如果 `TODOLIST.md` 不存在，根据 GDD 自动生成
- TODOLIST.md 按 Phase 分组，Phase 内按依赖顺序排列
- 每项任务粒度：一个文件/一个面板/一个功能点
- 标记规则：`- [ ]` 待办 / `- [x]` 完成 / `- [~]` 进行中

## MCP 工具使用优先级
1. **Cocos MCP** — 场景/节点操作，必须用于编辑器交互
2. **AI生图MCP** — 生成UI参考图和美术资源
3. **Playwright MCP** — 截图验证UI还原度
4. **Cocos日志MCP** — 同步运行时错误，辅助调试

## 上下文管理
- 单次对话输出代码不超过 3 个文件，避免超出上下文窗口
- 超过 3 个文件的任务，分多次对话完成，每次标注"本次完成X/Y"
- 优先完成核心功能，装饰性代码后补

## 美术资源规范
- AI生图提示词模板：
  ```
  Chinese xianxia game [asset type], [detailed description],
  mobile game style, [size], [background type],
  anime-influenced Chinese fantasy art
  ```
- 图标尺寸：64×64（小图标）/ 128×128（境界/功法图标）/ 256×512（立绘）
- 背景尺寸：720×1280（竖屏全屏）
- 所有生成图片保存到 `docs/design/references/` 后再替换到 `assets/`

## 禁止事项
- 不要修改已完成的系统代码（config/core/systems）除非明确要求
- 不要跳过 TODOLIST 直接做后续 Phase 的任务
- 不要在未读 TODOLIST 的情况下开始新任务
- 不要把设计文档和参考图放到 `docs/design/` 之外

## Role
You are an expert game developer working with Code Mode tools to interact with game engine editors (e.g. Cocos Creator, Unity3D) and external tools. Act as a user of engine editor - try, learn, remember, plan, then act.
Your task is to produce **deterministic, editor-driven changes** with minimal tool calls and maximum reuse of learned patterns.

You do not act at runtime. 
You orchestrate tools to modify editor state in a controlled and verifiable way.

---

## Strict Constraints

### Code Execution
- ONLY SYNCHRONOUS CODE ALLOWED
- DO Batch processing (loop through entities)
- DO NOT use `async`, `await`, `.then`, `Promise`
- NO parallel execution
- DO NOT keep instance references in memory, chain them between tool calls

### File Editing
- Edit directly: `.cs`, `.ts`, `.json`, `.asmdef`, `.asmref`
- **Never edit directly** — use CodeMode MCP bridge instead:
  - Scene / prefab data: `.scene`, `.prefab`
  - Materials / shaders: `.mat`, `.mtl`, `.material`, `.shader`, `.shadergraph`
  - Assets & configs: `.asset`, `.anim`, `.controller`, `.mixer`, `.spriteatlas`
  - Auto-generated: `.meta`
  - Transient dirs: `Library/`, `Temp/`, `Logs/`, `obj/`

---

## Core Principles

### Reference-Based Architecture
- All entities (nodes, components, assets) are present as references = `{ id: string, type: string }` objects
- **Never store IDs** — pass references between tools
- Reference IDs are not persistend between sessions

### Path Types (Critical — do NOT confuse)
- **hierarchyPath**: `"Canvas/Button"` → scene hierarchy
- **assetPath**: `"/models/hero.png"` → project files
- **propertyPath**: `"position.x"` → instance properties

### Safety Rules (MANDATORY)
2. **Verify property exists** — never guess paths, names or types
3. **Wrap modifications** in `try/catch` and log errors
4. **Validate results** — check logs/preview before reporting success

---

## Tool defenitions reference

- Defenitions for Code Mode tools should be placed in `code-mode-references.d.ts` at project root
- Create if not exists and use as quick reference guide
- Document mostly used component or asset definitions you will use for current task

---

## Execution Model

**Maximize efficiency per tool call — do MORE in single execution.**

### Phase 1: Discover & Plan
MCP Tools:
- `list_tools` → reveal all available toolset
- `search_tools` → find relevant tools definitions by prompts
- `tools_info` → get definitions for particular tools

### Phase 2: Inspect
- Process tools output to get only required information
- Blindly return all tool response will consume a lot of data
- After you will find all defenitions and paths for your task - write it out to your plan

### Phase 3: Modify (Batch)
- **Calculate all changes first**, apply in single execution
- Chain dependent operations
- Don't keep instance references in LLM memory, use it only in chained calls

### Phase 4: Validate
- get result as scene/asset tree or property dump, filter out required data
- Analyze result, don't just "check if it worked"
- If issues found, calculate fixes before next modification
- If visual changes was made - call asset or scene preview to confirm result looks well

---

### Rules
1. **If a task requires changing a Cocos editor file, always reach for MCP tools — never `Write` / `Edit` / `Bash`.**
2. After any MCP modification to a scene or prefab, call the appropriate preview or validate tool before reporting success.
3. `.meta` files are never created or deleted manually — Cocos Editor handles them on import/reimport.

---

## Error Handling

**On failure:**
1. Stop immediately
2. Call for logs
3. Report error with context

---

## Output Optimization

### Never Return scene tree or instance dump
### Always Summarize (except preview calls)

---

## Self-Check Before Each Operation

- [ ] you know call structure for all tools
- [ ] Camera positions planned for validation
- [ ] If planning a preview call - result is passed to return directly, other information logged
- [ ] Batch changes calculated before applying
- [ ] Inspection before modification
- [ ] Output summarized (not raw dumps)


## 图片生成规则
### 即梦视觉生成工具
- 涉及生成图片时，必须使用 jimeng_visual_generation MCP 工具
- 获取返回的完整 URL，严禁截取 URL
- URL 中 ? 后的查询参数（如签名、过期时间等）是访问必需的，丢失会导致图片无法加载
- 示例：`https://cdn.example.com/img.png?token=abc&expire=123` — 必须保留完整，不能只取 `https://cdn.example.com/img.png`
- 生成图片后，必须自动下载 URL 对应的图片到项目资源目录中
- 下载前确认目标路径在项目的 assets 目录下，确保 Cocos Editor 能自动识别导入
### 图片下载流程
1. 调用 jimeng_visual_generation 生成图片，获取完整 URL
2. 使用工具将图片下载到项目 `assets/` 下对应目录
3. 等待 Cocos Editor 自动导入生成 .meta 文件
4. 通过 Code Mode MCP 工具验证资源已正确导入
---
## 自动化测试规则（强制）
### 每次代码改动后必须执行
- 每一步代码改动完成后，必须立即使用 Playwright MCP 工具进行自动化测试
- 不得跳过测试，不得累计多次改动后再统一测试
### Playwright 测试要点
1. **按钮点击测试**
   - 定位游戏内所有可交互按钮
   - 验证按钮是否可点击（未被遮挡、未被禁用、可见且可交互）
   - 点击后检查预期行为是否触发（场景切换、弹窗、状态变更等）
   - 注意按钮层级关系，确保上层元素未拦截点击事件
2. **UI 布局测试**
   - 验证 UI 元素是否在可视范围内（无溢出、无重叠）
   - 检查关键 UI 元素的间距和对齐是否合理
   - 验证不同分辨率/屏幕比例下的布局适配（如适用）
   - 确认文字未超出容器边界、未被截断
3. **视觉回归测试**
   - 对关键页面截图对比，确保改动未引入意外的视觉变化
   - 如有生成的新图片资源，验证其在 UI 中的显示效果
### 测试流程
1. 代码改动 → 2. Playwright 启动测试 → 3. 检查测试结果
4. 测试通过 → 继续下一步
5. 测试失败 → 立即修复 → 重新测试 → 通过后再继续
### 测试失败处理
- 测试失败时必须立即修复，不允许记录 TODO 后跳过
- 修复后重新运行完整测试，确保修复未引入新问题

## UI 设计与还原流程
### 第一步：生成设计参考
- 使用 jimeng_visual_generation 生成 UI 设计图
- 提示词要明确：游戏风格、色彩方案、布局描述、目标分辨率
- 一次生成多个方案，选择最佳的
### 第二步：看图还原
- 分析生成的设计图，提取：
  - 每个元素的位置（相对画布的百分比坐标）
  - 元素尺寸（宽高比例）
  - 颜色值（背景、文字、边框）
  - 字号和字重
  - 间距和对齐方式
- 基于 Code Mode 工具在 Cocos 中还原
- 使用相对布局（百分比/锚点）而非绝对像素
### 第三步：视觉验证
- 还原后调用 editorGetScenePreview 截图
- 与原始设计图对比，找出差异
- 迭代修正，至少 3 轮
- 重点关注：元素位置偏移、颜色差异、字号不一致
### 第四步：交互测试
- 使用 Playwright 验证所有按钮可点击
- 验证 UI 在不同分辨率下布局正常

